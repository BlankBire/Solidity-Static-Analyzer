\documentclass[10pt, a4paper]{article}

% --- Gói cần thiết ---
\usepackage[utf8]{inputenc}
\usepackage[T5]{fontenc}
\usepackage{float}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{booktabs}
\usepackage{geometry}
\usepackage{authblk}
\usepackage{abstract}
\usepackage{tikz} 
\usetikzlibrary{shapes, arrows.meta, positioning, fit, backgrounds, calc}
\usepackage{tabularx}
\usepackage{pgfplots}
\pgfplotsset{compat=1.17}

% --- Thiết lập Geometry ---
\geometry{
    a4paper,
    top=1in,
    bottom=1in,
    left=1.5in,
    right=1.5in
}

% --- Thiết lập thụt dòng và khoảng cách đoạn (Block Style) ---
\setlength{\parindent}{0pt}      % Không thụt đầu dòng
\setlength{\parskip}{1em}        % Khoảng cách giữa các đoạn văn

% --- Tùy chỉnh Tiêu đề và Abstract ---
\renewcommand{\abstractname}{ABSTRACT}
\setlength{\absleftindent}{0.0in}
\setlength{\absrightindent}{0.0in}
\renewcommand{\Affilfont}{\itshape\small}

% --- Bắt đầu tài liệu ---
\begin{document}

\title{\textbf{Solidify: Real-Time Static Analysis for Solidity in IDE}}
\author{Nguyen Huu Canh\textsuperscript{1}, Sit Khai Dong\textsuperscript{2}, Tran Tuan Dung\textsuperscript{3}}
\affil{\textsuperscript{1,2,3}University of Information Technology, Vietnam National University\\Ho Chi Minh City, Vietnam}
\affil{\textsuperscript{1}23520166@gm.uit.edu.vn, \textsuperscript{2}23520299@gm.uit.edu.vn, \textsuperscript{3}dungtrt@uit.edu.vn}
\date{}
\maketitle

\begin{abstract}
Smart contracts underpin decentralized finance (DeFi) and Web3 applications, stewarding billions of dollars in value. Security defects in Solidity code can cause catastrophic losses. While standard development environments provide robust syntax highlighting and compilation checks, they often lack immediate feedback on security vulnerabilities. This leaves a blind spot during the coding phase, forcing developers to rely on slow, external audits later in the pipeline. This paper introduces \textbf{Solidify}, a Visual Studio Code extension that delivers real-time security static analysis directly inside the IDE. The tool adopts a hybrid strategy: it leverages \texttt{tree-sitter} to construct rich Abstract Syntax Trees (ASTs) while complementing them with high-performance regular expressions for rapid pattern checks. A modular rule architecture makes it straightforward to extend coverage across security, syntax, and naming conventions. Our evaluation on 20 real-world smart contracts compares Solidify against the standard Solidity extension by Juan Blanco. Results show that Solidify provides a significant leap in security detection capabilities ($F1 = 0.90$) compared to the Blanco's syntax-focused approach ($F1 = 0.73$), achieving exceptional recall (0.99) while maintaining high precision (0.83). Although Solidify operates at a higher latency of 538 ms ($\pm$ 19 ms) compared to compilation-based feedback (2447 ms $\pm$ 139 ms), it delivers results approximately 4.5$\times$ faster. This demonstrates that integrating security awareness directly into the editor is both performant and essential for modern smart contract development.
\end{abstract}

\textbf{Keywords:} Smart Contracts, Solidity, Static Analysis, Blockchain Security, Visual Studio Code, Abstract Syntax Tree

\section{Introduction}
The rise of blockchain technology ushered in a new era of decentralized applications with smart contracts at the core. Solidity has become the de facto language for authoring these contracts on Ethereum and other EVM-compatible chains. Yet the immutability of blockchain---the very property that ensures trust---cuts both ways: once deployed, smart contract code cannot be patched. Seemingly minor vulnerabilities can be exploited to drain digital assets worth millions of dollars \cite{atzei2017}.

Standard IDE tools, such as the widely used Solidity extension by Juan Blanco \cite{blanco2016}, have greatly improved the developer experience by providing syntax highlighting, snippet generation, and compilation integration. However, these tools generally focus on correctness (is the code syntactically valid?) rather than security (is the code safe?). They typically rely on the \texttt{solc} compiler for diagnostics, which means feedback is constrained by compilation times and limited to compiler errors.

We identify a critical gap: the lack of tools that raise security warnings with the same speed and convenience as syntax errors. To close this gap we built \textbf{Solidify}, a VS Code extension designed to be lightweight, responsive, and tightly integrated with the editor. Unlike standard extensions that wait for compilation, Solidify proactively scans for vulnerability patterns as the developer types.

Our main contributions are:
\begin{itemize}
    \item \textbf{Hybrid analysis architecture:} We present a design that combines AST-based reasoning with regex-based pattern detection to bypass the overhead of full compilation.
    \item \textbf{Modular rule system:} We architect the analyzer so that rules can be developed, maintained, and extended independently.
    \item \textbf{Comparative Evaluation:} We demonstrate that Solidify significantly outperforms the standard IDE baseline in security metrics while delivering faster feedback latency.
\end{itemize}

\section{Related Work}
Security analysis of smart contracts has attracted significant research. Existing tools generally fall into three categories: Static Analysis, Dynamic/Symbolic Execution, and IDE Tooling.

	\textbf{Static Analysis Frameworks:} These tools inspect source code without execution. \textbf{Slither} \cite{feist2019} converts Solidity to an intermediate representation (SlithIR) to run data-flow and taint analysis. It is fast for a CI tool but too slow for keystroke-level feedback. \textbf{Securify} \cite{tsankov2018}, developed at ETH Zurich, employs formal verification using dependency patterns to prove compliance with security properties. While highly precise, Securify acts as a validator rather than a real-time assistant, requiring complete and compilable code to function effectively.

	\textbf{Symbolic and Dynamic Analysis:} \textbf{Mythril} \cite{mueller2018} and \textbf{Manticore} \cite{mossberg2018} represent the heavyweight class of analysis. Mythril uses concolic analysis to explore attackable states. Manticore, developed by Trail of Bits, is a symbolic execution framework that can navigate complex execution paths to find deep logic bugs. These tools are computationally expensive, often taking minutes or hours to run, making them suitable for final audits but impractical for the interactive development loop.

	\textbf{IDE Extensions:} The developer's primary interface is the IDE. The most popular tool is \textbf{Juan Blanco's Solidity extension} \cite{blanco2016}. It wraps the official \texttt{solc} compiler to provide diagnostics. While it effectively catches syntax errors (e.g., missing semicolons, type mismatches), it does not natively scan for security patterns like reentrancy or timestamp dependence.

Solidify aims to bridge this gap by bringing lightweight security intelligence (inspired by static analyzers) into the immediate environment of the IDE (like Blanco's extension).

\begin{table}[H]
    \centering
    \caption{Comparison of Analysis Approaches}
    \label{tab:related_work_comparison}
    \begin{tabularx}{\textwidth}{@{} l >{\raggedright\arraybackslash}X c c @{}}
        \toprule
        	\textbf{Tool} & \textbf{Methodology} & \textbf{Speed} & \textbf{Target Phase} \\
        \midrule
        Slither & Static Analysis (IR) & Moderate & CI / Pre-commit \\
        Securify & Formal Verification & Slow & Audit \\
        Manticore & Symbolic Execution & Very Slow & Deep Audit \\
        J. Blanco Ext. & Compiler Wrapper & Fast & Coding (Syntax) \\
        \midrule
        	\textbf{Solidify} & \textbf{Hybrid (AST + Regex)} & \textbf{Instant} & \textbf{Coding (Security)} \\
        \bottomrule
    \end{tabularx}
\end{table}

\section{Methodology}
Meeting real-time performance targets while maintaining accuracy requires a hybrid and modular design.

\subsection{Hybrid Analysis Architecture}
Solidify blends two complementary analysis techniques:
\begin{enumerate}
    \item \textbf{AST-based analysis:} We rely on the \texttt{tree-sitter} library \cite{brunsfeld2018} and the \texttt{tree-sitter-solidity} grammar. Unlike the standard extension which may wait for a full AST from the compiler, \texttt{tree-sitter} builds partial ASTs incrementally, allowing for sub-millisecond updates even on broken code.
    \item \textbf{Regex-based analysis:} For localized patterns, regular expressions are used to instantly flag dangerous keywords (\texttt{tx.origin}, \texttt{selfdestruct}) without the need for semantic understanding.
\end{enumerate}

\subsection{System Architecture}
Solidify follows a five-layer architecture designed for isolation and speed.
\begin{itemize}
    \item \textbf{Layer 1 - VS Code Integration:} Anchors the tool to the IDE lifecycle.
    \item \textbf{Layer 2 - Core Analysis Engine:} Orchestrates the analysis pipeline.
    \item \textbf{Layer 3 - AST/CFG Processing:} Constructs structural views using \texttt{tree-sitter}.
    \item \textbf{Layer 4 - Enhanced Rules Engine:} Contains the security and quality rules.
    \item \textbf{Layer 5 - Advanced Diagnostic Display:} Surfaces findings to the user.
\end{itemize}

\begin{figure}[H]
    \centering
    \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}[
        block/.style={rectangle, draw, rounded corners, minimum height=3.2em, minimum width=5.4em, text centered, align=center, fill=white, font=\small},
        group/.style={draw, dashed, inner sep=14pt, rounded corners, fill=gray!5},
        arrow/.style={-{Latex[length=3mm]}, thick},
        lab/.style={font=\scriptsize, inner sep=1pt, text=black, fill=white}
    ]

    % Node coordinates for balanced layout with additional spacing
    \node[block]                         (ext)       at (4.2,-4.0){VS Code\\Event Hooks};
    \node[block]                         (core)      at (4.2,3.4)  {Core Analysis\\Pipeline};
    \node[block]                         (parser)    at (11.8,3.4) {AST Parser\\Module};
    \node[block]                         (semantic)  at (11.8,-0.4){Semantic Analyzer\\Module};
    \node[block]                         (cfg)       at (11.8,-4.4){CFG Builder\\Module};
    \node[block]                         (crossFile) at (18.2,3.4) {Cross-File\\Analysis Rules};
    \node[block]                         (astRule)   at (18.2,-0.4){AST-Based\\Rules};
    \node[block]                         (cfgRule)   at (18.2,-4.4){CFG-Based\\Rules};
    \node[block]                         (diag)      at (23.8,-0.4){Enhanced\\Diagnostic System};

    % Data flow arrows with annotations
    \draw[arrow] (ext) -- node[lab, pos=0.55, sloped, above, xshift=-6pt]{Document events} (core);
    \draw[arrow] (core) -- node[lab, pos=0.55, sloped, above]{Source snapshot} (parser);
    \draw[arrow] (parser) -- node[lab, pos=0.53, sloped, above, allow upside down, yshift=6pt]{AST nodes} (semantic);
    \draw[arrow] (semantic) -- node[lab, pos=0.47, sloped, above, allow upside down, yshift=6pt]{Control-flow seeds} (cfg);
    \draw[arrow] (parser) -- node[lab, pos=0.5, sloped, above]{Project graph} (crossFile);
    \draw[arrow] (parser) -- node[lab, pos=0.52, sloped, above]{Syntax tree} (astRule);
    \draw[arrow] (semantic) -- node[lab, pos=0.55, sloped, above]{Symbol tables} (astRule);
    \draw[arrow] (semantic) -- node[lab, pos=0.48, sloped, above]{Data-flow facts} (cfgRule);
    \draw[arrow] (cfg) -- node[lab, pos=0.5, sloped, above]{CFG edges} (cfgRule);
    \draw[arrow] (crossFile) -- node[lab, pos=0.52, sloped, above]{Workspace diagnostics} (diag);
    \draw[arrow] (astRule) -- node[lab, pos=0.5, sloped, above]{AST findings} (diag);
    \draw[arrow] (cfgRule) -- node[lab, pos=0.5, sloped, above]{CFG findings} (diag);

    % Background layer groups
    \begin{scope}[on background layer]
        \node[group, fit=(ext)] (l1) {};
        \node[group, fit=(core)] (l2) {};
        \node[group, fit=(parser) (semantic) (cfg)] (l3) {};
        \node[group, fit=(crossFile) (astRule) (cfgRule)] (l4) {};
        \node[group, fit=(diag)] (l5) {};
    \end{scope}
    \end{tikzpicture}
    }
    \caption{Solidify System Architecture}
    \label{fig:architecture}
\end{figure}

\section{Results}

We evaluate Solidify along two key dimensions: analysis latency and security detection accuracy. Our assessment compares Solidify against Juan Blanco's Solidity extension (v0.0.176), the most widely adopted VS Code tool for Solidity development, which relies on the \texttt{solc} compiler for diagnostics.

\subsection{Experimental Setup}

\textbf{Dataset:} We collected 30 real-world Solidity smart contracts of varying size and complexity, sampled from popular open-source projects on GitHub. The contracts range from simple token implementations to complex DeFi protocols, ensuring diversity in code patterns and potential vulnerability types.

\textbf{Baseline Configuration:} The baseline tool uses default \texttt{solc} compiler integration for diagnostics, representing the standard development experience for most Solidity developers.

\textbf{Hardware:} All experiments were conducted on identical hardware configurations (CPU: Intel i9-14900HX, RAM: 32 GB) to ensure fair comparison and reproducible results.

\begin{table}[H]
    \centering
    \caption{Comparison of Solidify vs. Blanco Extension Performance and Accuracy}
    \label{tab:comparison}
    \begin{tabularx}{0.95\textwidth}{@{} X >{\centering\arraybackslash}X >{\centering\arraybackslash}X @{}}
        \toprule
        \textbf{Metric} & \textbf{Solidify} & \textbf{Blanco Extension} \\
        \midrule
        Analysis Latency & 538ms ($\pm$ 19ms) & 2447ms ($\pm$ 139ms) \\
        Precision & 0.83 & 1.00 \\
        Recall & 0.99 & 0.58 \\
        F1 Score & 0.90 & 0.73 \\
        False Positive Rate & 0.17 & 0.00 \\
        \bottomrule
    \end{tabularx}
\end{table}

\subsection{Response Latency}

We measured the elapsed time from a keystroke event to the appearance of diagnostic feedback in the IDE interface. This metric directly impacts developer experience and workflow fluidity.

Solidify achieves an average latency of \textbf{538 ms ($\pm$ 19 ms)}, whereas the Blanco extension averages \textbf{2447 ms ($\pm$ 139 ms)}. The Blanco's reliance on triggering the Solidity compiler introduces significant overhead through process spawning, full parsing, and type checking. In contrast, Solidify's incremental \texttt{tree-sitter} parsing enables feedback delivery approximately 4.5$\times$ faster, ensuring the UI remains responsive and never interrupts the coding flow.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            ybar,
            bar width=35pt,
            width=0.6\textwidth,
            height=6cm,
            enlarge x limits=0.5,
            symbolic x coords={Solidify, Blanco Extension},
            xtick={Solidify, Blanco Extension}, 
            ylabel={Latency (ms) - Lower is Better},
            ymin=0, 
            ymax=3000,
            grid=major,
            nodes near coords,
            every node near coords/.append style={font=\bfseries},
            bar shift=0pt 
        ]
        \addplot[fill=blue!60, draw=black] coordinates {
            (Solidify, 538)
        };
        \addplot[fill=gray!60, draw=black] coordinates {
            (Blanco Extension, 2447)
        };
        \end{axis}
    \end{tikzpicture}
    \caption{Average Diagnostic Latency Comparison}
    \label{fig:latency_comparison}
\end{figure}

\subsection{Accuracy Metrics}

We evaluated both tools on their ability to identify known security vulnerabilities in our dataset, including reentrancy patterns, unchecked external calls, timestamp dependence, and unsafe authentication mechanisms.

The Blanco tool, designed primarily for syntax correctness, demonstrates strong precision (1.00) but limited recall (0.58), resulting in an $F1$ score of 0.73. While it successfully flags compilation warnings with zero false positives, it misses many security-specific patterns. The perfect precision reflects that flagged issues are indeed problems, but these are predominantly syntax errors rather than security vulnerabilities.

Solidify substantially improves security detection capabilities, achieving an $F1$ score of \textbf{0.90} with high precision (0.83) and exceptional recall (0.99). The tool successfully catches nearly all security vulnerabilities in the dataset while maintaining acceptable false positive rates (0.17). While not matching the exhaustive coverage of offline audit tools like Slither or Manticore, Solidify represents a significant advancement over standard IDE capabilities, catching critical vulnerabilities during active development.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            ybar,
            bar width=20pt,
            width=0.95\textwidth,
            height=7cm,
            enlarge x limits=0.15,
            symbolic x coords={Precision, Recall, F1 Score, False Positive Rate},
            xtick=data,
            ymin=0, ymax=1.0,
            ylabel={Score},
            grid=major,
            nodes near coords={\pgfmathprintnumber[fixed, fixed zerofill, precision=2]{\pgfplotspointmeta}},
            every node near coords/.append style={font=\footnotesize},
            legend style={at={(0.5,-0.15)}, anchor=north, legend columns=-1}
        ]
        
        \addplot[fill=blue!60, draw=black] coordinates {
            (Precision,0.83) 
            (Recall,0.99) 
            (F1 Score,0.90) 
            (False Positive Rate,0.17)
        };
        
        \addplot[fill=gray!60, draw=black] coordinates {
            (Precision,1.00) 
            (Recall,0.58) 
            (F1 Score,0.73) 
            (False Positive Rate,0.00)
        };
        
        \legend{Solidify, Blanco Extension}
        \end{axis}
    \end{tikzpicture}
    \caption{Comparative Analysis of Accuracy Metrics}
    \label{fig:accuracy_comparison}
\end{figure}

\section{Discussion}

\subsection{Strengths}

Our evaluation demonstrates that Solidify successfully addresses a critical gap in smart contract development tooling by bringing security-aware static analysis into the real-time IDE workflow without compromising performance. The hybrid AST-regex architecture proves particularly effective: \texttt{tree-sitter}'s incremental parsing delivers sub-second response times crucial for maintaining developer flow, while pattern-based rules catch common vulnerabilities instantly. This represents a paradigm shift from the traditional compile-check-fix cycle to continuous security feedback.

The tool's modular rule architecture offers significant practical advantages. Security patterns can be independently developed, tested, and deployed without modifying the core analysis engine. This extensibility ensures that Solidify can evolve alongside emerging vulnerability classes and Solidity language updates. Furthermore, by integrating directly into the developer's primary workspace, Solidify reduces context switching and lowers the barrier to security awareness---developers receive guidance at the moment they write potentially vulnerable code rather than discovering issues hours or days later during external audits.

\subsection{Limitations}

Despite these strengths, Solidify has several limitations that constrain its current capabilities. The most significant is limited semantic depth: our hybrid approach excels at pattern matching but cannot perform deep data-flow analysis or symbolic reasoning. Complex vulnerabilities requiring cross-function taint tracking or multi-transaction attack scenarios remain beyond the tool's detection scope. This means Solidify should complement, not replace, thorough security audits using tools like Slither or Manticore.

The current rule coverage, while addressing common vulnerability patterns, is not exhaustive. Our evaluation focused on well-documented vulnerability classes, but the rapidly evolving DeFi ecosystem continuously produces novel attack vectors. The tool's effectiveness depends on rule maintenance and expansion, requiring ongoing community contribution or dedicated curation. Additionally, the false positive rate of 0.17, while reasonable for a real-time tool, may cause alert fatigue if developers encounter frequent incorrect warnings during active development. Balancing sensitivity with precision remains an ongoing challenge, particularly for rules that require semantic context our lightweight analysis cannot fully capture.

\section{Conclusion}

This paper presented Solidify, a real-time static analysis extension that integrates security intelligence directly into the Solidity development workflow within Visual Studio Code. Our primary contributions include a hybrid AST-regex analysis architecture that bypasses compilation overhead, a modular rule system enabling independent extension of security checks, and empirical validation demonstrating practical viability. Experimental results show that Solidify delivers diagnostic feedback 4.5$\times$ faster than the industry-standard extension (538 ms vs. 2447 ms) while substantially improving security detection capabilities ($F1 = 0.90$ vs. $0.73$). These findings validate that shifting security analysis left to the earliest moment in the development lifecycle is both technically feasible and practically valuable for smart contract development.

However, several limitations present opportunities for future enhancement. To address the semantic depth gap, we plan to integrate lightweight data-flow analysis that can track variable states across function boundaries without full symbolic execution overhead. Expanding rule coverage will involve establishing a community-driven rule repository with standardized testing frameworks to ensure quality and reduce false positives. We also intend to incorporate machine learning-based pattern recognition to detect novel vulnerability classes that lack explicit rules. Finally, cross-platform support beyond VS Code (such as JetBrains IDEs and Vim) and integration with CI/CD pipelines will extend Solidify's reach across the entire development lifecycle. These enhancements will position Solidify as a comprehensive security companion for smart contract developers.

\begin{thebibliography}{9}
\bibitem{atzei2017} N. Atzei, M. Bartoletti, and T. Cimoli, ``A survey of attacks on Ethereum smart contracts,'' in \textit{Principles of Security and Trust (POST)}, ser. Lecture Notes in Computer Science, vol. 10260. Cham: Springer, 2017, pp. 109--139.

\bibitem{feist2019} J. Feist, G. Grieco, and A. Groce, ``Slither: A static analysis framework for smart contracts,'' in \textit{IEEE International Conference on Blockchain (Blockchain)}, 2019, pp. 8--15.

\bibitem{tsankov2018} P. Tsankov, A. Dan, D. Drachsler-Cohen, A. Gervais, F. Bünzli, and M. Vechev, ``Securify: Practical security analysis of smart contracts,'' in \textit{Proceedings of the 2018 ACM SIGSAC Conference on Computer and Communications Security (CCS)}, 2018, pp. 67--82.

\bibitem{mueller2018} B. Mueller, ``Mythril: Security analysis tool for EVM bytecode,'' \textit{GitHub repository}, 2018. [Online]. Available: \url{https://github.com/ConsenSys/mythril}

\bibitem{mossberg2018} M. Mossberg, F. Manzano, E. Hennenfent, A. Groce, G. Grieco, J. Feist, T. Brunson, and A. Dinaburg, ``Manticore: A user-friendly symbolic execution framework for binaries and smart contracts,'' in \textit{2019 34th IEEE/ACM International Conference on Automated Software Engineering (ASE)}, 2019, pp. 1186--1189.

\bibitem{blanco2016} J. Blanco, ``Solidity extension for Visual Studio Code,'' VS Code Marketplace, 2016. [Online]. Available: \url{https://marketplace.visualstudio.com/items?itemName=JuanBlanco.solidity}

\bibitem{brunsfeld2018} M. Brunsfeld, ``Tree-sitter: An incremental parsing system for programming tools,'' \textit{GitHub repository}, 2018. [Online]. Available: \url{https://tree-sitter.github.io/tree-sitter/}
\end{thebibliography}

\end{document}